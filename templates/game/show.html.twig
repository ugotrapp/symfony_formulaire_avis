{% extends 'base.html.twig' %}

{% block title %}game{% endblock %}

{% block body %}
<div class="card border-info m-3" style="max-width: 15rem;">
<h5 class="card-title">{{game.nom}}</h5>
  <img src="{{ game.image }}" class="card-img-top" alt="...">
  <div class="card-body">
    <p class="card-text">Lorem ipsum dolor sit amet consectetur adipisicing elit. Repellat laudantium, expedita nisi molestias libero impedit.</p>
    <button type="btn" class="btn btn-success" ><a href="{{ path('game_show_avis', {'id': game.id}) }}">Voir les avis</a></button>
   
  </div>
</div>
{% endblock %}
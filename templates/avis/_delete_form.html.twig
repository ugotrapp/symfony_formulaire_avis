<form method="post" action="{{ path('avis_delete', {'id': avis.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avis.id) }}">
    <button class="btn">Delete</button>
</form>

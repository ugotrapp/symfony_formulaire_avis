<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
           {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
    </head>
    <body>


<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarColor03">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
                    {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_MEMBER') %}
                    
                        <a class="nav-link" href="{{ path('app_logout') }}"><font style="vertical-align: inherit;"><font
                                        style="vertical-align: inherit;">Déconnexion</font></font></a>
                    {% else %}
                        <a class="nav-link" href="{{ path('app_login') }}"><font style="vertical-align: inherit;"><font
                                        style="vertical-align: inherit;">Connexion</font></font></a>
                    {% endif %}
                    
               
                
      </ul>
      <div class="d-flex">
        {% if is_granted('ROLE_ADMIN') %}<h5>mode administrateur</h5>{% endif %}
        {% if is_granted('ROLE_MEMBER') %}<h5>espace membre</h5>{% endif %}
      </div>
     
    </div>
  </div>
</nav>


        {% block body %}{% endblock %}
    </body>
</html>
